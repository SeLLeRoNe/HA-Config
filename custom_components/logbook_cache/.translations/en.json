{
  "config": {
    "title": "Logbook Cache",
    "step": {
      "confirm": {
        "title": "Logbook Cache",
        "description": "Do you want to set up Logbook Cache?"
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single configuration is possible."
    }
  },
  "options": {
    "step": {
        "init": {
            "data": {
                "only_cache": "Show only cached entries (fast or nothing)",
                "cache_days": "Number of logbook days to cache"
            }
        }
    }
  }
}
