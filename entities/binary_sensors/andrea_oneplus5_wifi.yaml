---
platform: template
sensors:
  andrea_oneplus5_wifi:
    friendly_name: OnePlus 5 WiFi
    value_template: >-
      {% if state_attr('device_tracker.andrea_oneplus5', 'wifi_on') %}
        {{ state_attr('device_tracker.andrea_oneplus5', 'wifi_on') }}
      {% elif is_state('input_boolean.andrea_oneplus5_wifi', 'on') %}
        True
      {% elif is_state('input_boolean.andrea_oneplus5_wifi', 'off') %}
        False
      {% elif not is_state('input_boolean.andrea_oneplus5_wifi', 'unknown') %}
        unknown
      {% else %}
        False
      {% endif %}
    icon_template: >-
      {% if is_state('binary_sensor.andrea_oneplus5_wifi', 'on') %}
        mdi:wifi
      {% elif is_state('input_boolean.andrea_oneplus5_wifi', 'on') %}
        mdi:wifi
      {% else %}
        mdi:wifi-off
      {% endif %}
