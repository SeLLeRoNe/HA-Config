---
platform: template
sensors:
  andrea_honor8_wifi:
    friendly_name: Honor 8 WiFi
    value_template: >-
      {% if not is_state('sensor.andrea_honor8_wifi', '<unknown ssid>') %}
        True
      {% elif is_state('sensor.andrea_honor8_wifi', '<unknown ssid>') %}
        False
      {% else %}
        unknown
      {% endif %}
    icon_template: >-
      {% if is_state('binary_sensor.andrea_honor8_wifi', 'on') %}
        mdi:wifi
      {% elif is_state('input_boolean.andrea_honor8_wifi', 'on') %}
        mdi:wifi
      {% else %}
        mdi:wifi-off
      {% endif %}