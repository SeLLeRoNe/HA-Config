---
platform: template
sensors:
  andrea_honor8_charging:
    friendly_name: Honor 8 Charging
    value_template: >-
      {% if state_attr('device_tracker.andrea_honor8', 'charging') %}
        {{ state_attr('device_tracker.andrea_honor8', 'charging') }}
      {% elif is_state('input_boolean.andrea_honor8_charging', 'on') %}
        True
      {% elif is_state('input_boolean.andrea_honor8_charging', 'off') %}
        False
      {% elif not is_state('input_boolean.andrea_honor8_charging', 'unknown') %}
        unknown
      {% else %}
        False
      {% endif %}
    icon_template: mdi:power-plug