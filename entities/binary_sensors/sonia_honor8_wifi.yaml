---
platform: template
sensors:
  sonia_honor8_wifi:
    friendly_name: Honor 8 WiFi
    value_template: >-
      {% if not is_state('sensor.sonia_honor8_wifi', '<unknown ssid>') %}
        True
      {% elif is_state('sensor.sonia_honor8_wifi', '<unknown ssid>') %}
        False
      {% else %}
        unknown
      {% endif %}
    icon_template: >-
      {% if is_state('binary_sensor.sonia_honor8_wifi', 'on') %}
        mdi:wifi
      {% elif is_state('input_boolean.sonia_honor8_wifi', 'on') %}
        mdi:wifi
      {% else %}
        mdi:wifi-off
      {% endif %}