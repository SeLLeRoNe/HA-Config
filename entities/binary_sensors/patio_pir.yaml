---
platform: template
sensors:
  patio_pir:
    friendly_name: "Patio PIR"
    device_class: motion
    entity_id:
      - binary_sensor.patio_rf_pir
    value_template: "{{ expand('binary_sensor.patio_pir') | selectattr('state','eq','on') | list | count > 0 }}"
