---
alias: Master Bedroom - WXKG02LM Switch Left Single
initial_state: on
trigger:
  - platform: mqtt
    topic: 'zigbee2mqtt/master_bedroom_WXKG02LM'
condition:
  condition: and
  conditions:
    - condition: template
      value_template: "{{ trigger.payload_json['click'] == 'left' }}"
action:
  - service: light.toggle
    data:
      entity_id: light.master_bedroom_lamp_top_light
      brightness: 150
      rgb_color: [255,0,0]
