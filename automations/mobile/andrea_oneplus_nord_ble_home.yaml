---
id: '693182612101942'
alias: Mobile - Andrea - OnePlus Nord - Bluetooth LTE - Home
trigger:
  - platform: homeassistant
    event: start
  - platform: state
    entity_id: sensor.andrea_oneplus_nord_ble_transmitter
    to: 'Transmitting'
condition:
  condition: and
  conditions:
    - condition: state
      entity_id: sensor.andrea_oneplus_nord_ble_transmitter
      state: 'Transmitting'
action:
  - service: device_tracker.see
    data:
      dev_id: andrea_oneplus_nord_bt
      location_name: home
