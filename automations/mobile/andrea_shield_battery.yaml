---
alias: Mobile - NVIDIA Shield K1 - Battery
initial_state: on
trigger:
  - platform: time_pattern
    minutes: '*'
condition:
  condition: and
  conditions:
    - condition: template
      value_template: '{% if states.input_text.andrea_shield_battery is defined %} {{(as_timestamp(now()) - as_timestamp(states.input_text.andrea_shield_battery.last_changed)) > 43200 }} {% endif %}'
action:
  - service: input_text.set_value
    data_template:
      entity_id: input_text.andrea_shield_battery
      value: "unknown"
  - service: input_boolean.turn_off
    data_template:
      entity_id: input_boolean.andrea_shield_charging
  - service: input_boolean.turn_off
    data_template:
      entity_id: input_boolean.andrea_shield_wifi
