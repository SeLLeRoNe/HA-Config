---
alias: Mobile - Andrea - OnePlus 5 - Battery Low
initial_state: on
trigger:
  - platform: numeric_state
    entity_id: sensor.andrea_oneplus5_battery
    below: 20
condition:
  condition: and
  conditions:
    - condition: state
      entity_id: binary_sensor.andrea_oneplus5_charging
      state: 'off'
    - condition: state
      entity_id: person.andrea_iannucci
      state: 'home'
action:
  - service: notify.mobile_app_andrea_oneplus_5
    data_template:
      title: "Battery Alert"
      message: "Low Battery on OnePlus 5, please plug it!"
      data:
        color: "#FF66FF"
        sticky: "true"
        tag: tag
        ttl: 0
